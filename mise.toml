# Toolchain for this repository.
#
# yarn is not listed here on purpose. It is already pinned by the
# "packageManager" field in package.json and provided by corepack, and a
# second declaration would give the version two sources of truth.

[tools]
node = "22.23.2"
