node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# cypress
/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vs/

# coverage
coverage/

# Stylelint
.stylelintcache

# tsc
tsconfig.node.tsbuildinfo

#vite
vite.config.ts.timestamp-*

# wrangler
# .dev.vars holds the local values of what production gets from `wrangler secret`.
.wrangler/
.dev.vars
.dev.vars.*

# claude code
# Each worktree is a real checkout, so `git add -A` would otherwise pull them
# in as embedded repositories. The handoff notes next to them carry their own
# .gitignore.
.claude/worktrees/
